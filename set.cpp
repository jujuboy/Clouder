#include <iostream>
#include <array>
using namespace std;
int arr[100000];
bool condition (int a, int b) {
    for (int i=0; i<a; i++) {
        if (arr[i]==b) return false;
    }
    return true;
}
int main(void) {
    cin >> rep;
    repcache=rep;
    while (rep--) {
        cin >> inserto;
        if (condition(numofnums,inserto)==true) {
            
        }
    }
}
